package battle

// This file used to host the proximity-based encounter group selector
// (Manhattan + LOS). Packs are authored on the map now, so the encounter
// is exactly the engaged pack — no spatial clustering is needed. The
// file is kept as a place to land battle-helper utilities that don't
// belong in any of the other split files.
